# ==================
# KEYBINDINGS
# ==================
$mod = SUPER

# bindr = $mod, Super_L, exec, dms ipc call spotlight toggle

# === Application Launchers ===
# bind = $mod, T, exec, ghostty +new-window
# bind = $mod, return, exec, ghostty +new-window
bind = $mod, T, exec, kitty
bind = $mod, return, exec, kitty
bindd = $mod, F, file manager, exec, nautilus

# === DankMaterialShell === 
bind = $mod, S, exec, dms ipc  widget toggle launcherButton
bind = $mod, space, exec, dms ipc call spotlight toggle
bindd = $mod, B, open default browser, exec, xdg-open "https://"
bind = $mod, V, exec, dms ipc call clipboard toggle
bind = $mod, M, exec, dms ipc call processlist focusOrToggle
bind = $mod, comma, exec, dms ipc call settings focusOrToggle
bind = $mod, N, exec, dms ipc call notifications toggle
bind = $mod SHIFT, N, exec, dms ipc call notepad toggle
bind = $mod, Y, exec, dms ipc call dankdash wallpaper
bind = $mod SHIFT, X, exec, dms ipc call control-center toggle
bind = $mod SHIFT, R, exec, hyprctl reload
bind = CTRL ALT, DELETE, exec, dms ipc call powermenu toggle
# bind = $mod, W, exec, dms ipc call hypr toggleOverview


# === QuickShell === 
bind = $mod, W, exec, qs ipc call overview toggle
bind = $mod SHIFT, C, exec, dms color pick --autocopy

# === Cheat sheet ===
bind = $mod SHIFT, I, exec, dms ipc call keybinds toggle hyprland

# === Security ===
bind = CTRL ALT, L, exec, dms ipc call lock lock
bind = $mod SHIFT, E, exit
bind = CTRL SHIFT, escape, exec, dms ipc call processlist focusOrToggle

# === Audio Controls ===
bindel = , XF86AudioRaiseVolume, exec, dms ipc call audio increment 3
bindel = , XF86AudioLowerVolume, exec, dms ipc call audio decrement 3
bindl = , XF86AudioMute, exec, dms ipc call audio mute
bindl = , XF86AudioMicMute, exec, dms ipc call audio micmute

# === Brightness Controls ===
bindel = , XF86MonBrightnessUp, exec, dms ipc call brightness increment 5 ""
bindel = , XF86MonBrightnessDown, exec, dms ipc call brightness decrement 5 ""

# === Window Management ===
bind = $mod, Q, killactive
# bind = $mod, F, fullscreen, 1
bind = $mod SHIFT, F, fullscreen, fullscreen
bind = $mod SHIFT, T, togglefloating
bind = $mod, G, togglegroup

# === Focus Navigation ===
bind = $mod, left, movefocus, l
bind = $mod, down, movefocus, d
bind = $mod, up, movefocus, u
bind = $mod, right, movefocus, r
bind = $mod, H, movefocus, l
bind = $mod, J, movefocus, d
bind = $mod, K, movefocus, u
bind = $mod, L, movefocus, r

# === Window Movement ===
bind = $mod SHIFT, left, movewindow, l
bind = $mod SHIFT, down, movewindow, d
bind = $mod SHIFT, up, movewindow, u
bind = $mod SHIFT, right, movewindow, r
bind = $mod SHIFT, H, movewindow, l
bind = $mod SHIFT, J, movewindow, d
bind = $mod SHIFT, K, movewindow, u
bind = $mod SHIFT, L, movewindow, r

# Resize windows
binded = $mod CTRL, H, resize left (-50), resizeactive, -50 0
binded = $mod CTRL, L, resize right (+50), resizeactive, 50 0
binded = $mod CTRL, K, resize up (-50), resizeactive, 0 -50
binded = $mod CTRL, J, resize down (+50), resizeactive, 0 50

# Swap windows
bindd = $mod ALT, H, swap window left, swapwindow, l
bindd = $mod ALT, L, swap window right, swapwindow, r
bindd = $mod ALT, K, swap window up, swapwindow, u
bindd = $mod ALT, J, swap window down, swapwindow, d


# Cycle windows; if floating bring to top
bindd = ALT, tab, cycle next window, cyclenext
bindd = ALT, tab, bring active to top, bringactivetotop

# === Column Navigation ===
bind = $mod, Home, focuswindow, first
bind = $mod, End, focuswindow, last

# === Monitor Navigation ===
bind = $mod CTRL, left, focusmonitor, l
bind = $mod CTRL, right, focusmonitor, r

bind = $mod CTRL, H, focusmonitor, l
bind = $mod CTRL, J, focusmonitor, d
bind = $mod CTRL, K, focusmonitor, u
bind = $mod CTRL, L, focusmonitor, r

# === Move to Monitor ===
bind = $mod SHIFT CTRL, left, movewindow, mon:l
bind = $mod SHIFT CTRL, down, movewindow, mon:d
bind = $mod SHIFT CTRL, up, movewindow, mon:u
bind = $mod SHIFT CTRL, right, movewindow, mon:r

bind = $mod SHIFT CTRL, H, movewindow, mon:l
bind = $mod SHIFT CTRL, J, movewindow, mon:d
bind = $mod SHIFT CTRL, K, movewindow, mon:u
bind = $mod SHIFT CTRL, L, movewindow, mon:r

# === Workspace Navigation ===
bind = $mod, Page_Down, exec, hyprnome
bind = $mod, Page_Up, exec, hyprnome --previous
bind = $mod, D, exec, hyprnome
bind = $mod, A, exec, hyprnome --previous
bind = $mod SHIFT, Page_Down, exec, hyprnome --move
bind = $mod SHIFT, Page_Up, exec, hyprnome --previous --move
bind = $mod SHIFT, D, exec, hyprnome --move
bind = $mod SHIFT, A, exec, hyprnome --previous --move

# === Mouse Wheel Navigation ===
bind = $mod, mouse_down, exec, hyprnome
bind = $mod, mouse_up, exec, hyprnome --previous
bind = $mod CTRL, mouse_down, exec, hyprnome --move
bind = $mod CTRL, mouse_up, exec, hyprnome --previous --move

# === Numbered Workspaces ===
bind = $mod, 1, workspace, 1
bind = $mod, 2, workspace, 2
bind = $mod, 3, workspace, 3
bind = $mod, 4, workspace, 4
bind = $mod, 5, workspace, 5
bind = $mod, 6, workspace, 6
bind = $mod, 7, workspace, 7
bind = $mod, 8, workspace, 8
bind = $mod, 9, workspace, 9

# === Move to Numbered Workspaces ===
bind = $mod SHIFT, 1, movetoworkspace, 1
bind = $mod SHIFT, 2, movetoworkspace, 2
bind = $mod SHIFT, 3, movetoworkspace, 3
bind = $mod SHIFT, 4, movetoworkspace, 4
bind = $mod SHIFT, 5, movetoworkspace, 5
bind = $mod SHIFT, 6, movetoworkspace, 6
bind = $mod SHIFT, 7, movetoworkspace, 7
bind = $mod SHIFT, 8, movetoworkspace, 8
bind = $mod SHIFT, 9, movetoworkspace, 9

# === Column Management ===
bind = $mod, bracketleft, layoutmsg, preselect l
bind = $mod, bracketright, layoutmsg, preselect r

# === Sizing & Layout ===
bind = $mod, R, layoutmsg, togglesplit
bind = $mod CTRL, F, resizeactive, exact 100%

# === Move/resize windows with mainMod + LMB/RMB and dragging ===
bindmd = $mod, mouse:272, Move window, movewindow
bindmd = $mod, mouse:273, Resize window, resizewindow

# === Move/resize windows with mainMod + LMB/RMB and dragging ===
bindd = $mod, code:20, Expand window left, resizeactive, -100 0
bindd = $mod, code:21, Shrink window left, resizeactive, 100 0

# === Manual Sizing ===
binde = $mod, minus, resizeactive, -10% 0
binde = $mod, equal, resizeactive, 10% 0
binde = $mod SHIFT, minus, resizeactive, 0 -10%
binde = $mod SHIFT, equal, resizeactive, 0 10%

# === Screenshots ===
bind = , Print, exec, dms screenshot
bind = CTRL, Print, exec, dms screenshot full
bind = ALT, Print, exec, dms screenshot window

# === System Controls ===
bind = $mod SHIFT, P, dpms, toggle
